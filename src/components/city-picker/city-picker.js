import React, { Component } from "react";

class CityPicker extends Component {
  render() {
    return (
      <div>
        CityPicker with select
      </div>
    );
  }
}

export default CityPicker;