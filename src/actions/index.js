import {
  fetchCities,
  fetchTimetable,
  setAvailableTime,
} from './data-actions';

import {
  selectCity,
  selectTime,
  selectDay,
  selectName,
  selectPhone,
  submitData,
} from './form-actions';

import {
  validateDay,
  validateName,
  validatePhone,
  validateTime,
} from './validate-actions';

export {
  fetchCities,
  fetchTimetable,
  setAvailableTime,
  selectCity,
  selectTime,
  selectDay,
  selectName,
  selectPhone,
  submitData,
  validateDay,
  validateName,
  validatePhone,
  validateTime,
};
